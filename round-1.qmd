---
format:
  revealjs:
    navigation: grid
    transition: convex
    controls: true
    css: style.css
    embed-resources: true
---

```{r setup}
library(readxl)
library(dplyr)
library(huxtable)
library(htmltools)

source("functions.R")

quiz <- read.csv("quiz.csv", quote = "\'")
rounds <- read.csv("rounds.csv")
```


# `r round_name(1)` - RECAP {.question .recap background-image="img/backgrounds/question.png"}

`r recap_round(1)`

